import 'package:flutter/material.dart';

class ShopByMoodScreen extends StatelessWidget {
  const ShopByMoodScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Shop by Mood')),
      body: const Center(
        child: Text('Shop by mood feature coming soon!'),
      ),
    );
  }
}
